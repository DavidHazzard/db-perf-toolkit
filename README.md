# db-perf-toolkit
A CLI and library for database performance analysis. Point it at a SQL Server or PostgreSQL instance and it surfaces slow queries, missing indexes, unused indexes, table bloat, and locking issues. Think of it as a lightweight, open-source alternative to expensive DBA tools.
